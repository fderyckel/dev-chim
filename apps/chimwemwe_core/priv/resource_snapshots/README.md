# Production-core Ash resource snapshots

This directory is intentionally empty in Phase 1 Slice 1E. Generated snapshots begin with the first explicitly authorized persistent Ash resource and are drift-checked with its migrations.
