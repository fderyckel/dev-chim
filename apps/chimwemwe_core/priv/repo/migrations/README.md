# Production-core migrations

This directory is intentionally empty in Phase 1 Slice 1E because the production Ash domain has no resources. The first migration must arrive with an explicitly authorized persistent-resource slice and follow `docs/development/migrations.md`.
