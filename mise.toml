min_version = "2026.9.6"

[tools]
erlang = "29.0.5"
elixir = "1.20.3-otp-29"
python = "3.14.5"
node = "24.15.0"
uv = "0.12.13"

[tasks.bootstrap]
description = "Install dependencies and prepare the local workspace"
run = "./bin/bootstrap"

[tasks.check]
description = "Run the complete local quality and test suite"
run = "make check"
